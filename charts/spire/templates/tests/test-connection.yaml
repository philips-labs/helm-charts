apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spire.fullname" . }}-test-connection"
  labels:
    {{- include "spire.server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spire.fullname" . }}-server:{{ .Values.service.port }}']
  restartPolicy: Never
