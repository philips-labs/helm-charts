apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spire.fullname" . }}-test-connection"
  labels:
    {{- include "spire.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spire.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
